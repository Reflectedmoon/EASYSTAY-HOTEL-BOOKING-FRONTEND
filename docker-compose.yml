version: '3.8'

services:
  yisu-app:
    build: .
    container_name: yisu-hotel-app
    ports:
      - "80:3000"
    volumes:
      - ./data.db:/app/data.db
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
